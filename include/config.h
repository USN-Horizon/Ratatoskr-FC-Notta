#pragma once

// Config settings that are used across the entire project

// For example, we could define if we're compiling for debugging or release
#define BUILD_DEBUG

/**
 *  USE_FAKE_IMPLEMENTATIONS:
 *  HAL will create hardware objects with mock (custom) implementations.
 */
#define USE_MOCK_IMPLEMENTATIONS